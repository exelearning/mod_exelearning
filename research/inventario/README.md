# inventario/

Listas estructuradas derivadas de las fichas REPO. **Sin opiniones**: sólo taxonomía.

- `apis/` — Firmas de funciones públicas (`lib.php`) de los plugins analizados.
- `modelos-datos/` — Esquemas de tablas (`install.xml` extraídos y anotados).

Las interpretaciones viven en `../analisis/`, no aquí. Esta carpeta se irá rellenando
en tareas posteriores (no incluida en el bootstrap inicial salvo este README).
