# analisis/

**Interpretaciones**. Aquí se compara, sintetiza, hipotetiza. Toda afirmación cita
fuentes en `../fuentes/`. Las interpretaciones se marcan con `[INTERPRETACION]`, las
conjeturas con `[HIPOTESIS]`, los huecos con `[PENDIENTE]`.

- `notas/` — `AN-NNN.md`.
- `matrices/` — matrices de decisión en YAML.

Las decisiones derivadas viven en `../decisiones/adr/`.
