# tareas/

Trabajo operativo del proyecto.

- `backlog/` — `TAREA-NNN.yaml`, una por unidad de trabajo accionable.
- `diario/` — `YYYY-MM-DD.yaml`, append-only.
- `preguntas/` — `PREG-NNN.yaml`, abiertas hasta que se resuelven.

El estado global está en [`../status.yaml`](../status.yaml).
