id: TAREA-008
titulo: "RIE-010: aplicar guard de origen al puente legacy postMessage"
estado: Done
fecha: 2026-06-01
agente: codex
prioridad: alta
objetivo: >-
  Corregir `amd/src/editor_modal.js::handleLegacyBridgeMessage()` para validar
  `event.source` y `event.origin` igual que el handler moderno, y regenerar
  `amd/build/` con `grunt amd`.
pasos:
  - "Aplicar el guard de origen al handler legacy."
  - "Regenerar `amd/build/editor_modal.min.js` con el toolchain Moodle (`grunt amd`)."
  - "Ejecutar `node --check` sobre fuente y build."
  - "Ejecutar `vendor/bin/phpcs --standard=moodle` si se toca PHP de apoyo."
relacionados:
  - RIE-010
  - DEC-0016
  - DEC-0018
criterios_aceptacion:
  - "El handler legacy rechaza mensajes cuyo source/origin no coincide con el iframe esperado."
  - "`amd/src` y `amd/build` quedan sincronizados por `grunt amd`."
  - "La CI de grunt no detecta divergencias."
notas: >-
  Implementado en `amd/src/editor_modal.js` con helper común de validación
  `event.source`/`event.origin` para bridge moderno y legacy. `amd/build/`
  regenerado con Moodle 5.2beta (`../moodle`, Node 22.22.3,
  `grunt amd --root=public/mod/exelearning`) usando una copia temporal real del
  plugin dentro del árbol Moodle para evitar problemas de symlink con ESLint.
