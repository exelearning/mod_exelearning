fecha: 2026-06-01
sesion: behat-ci-hotfix
agente: codex
resumen: >-
  Corrección del fallo Behat del PR #8: el escenario multipágina `@javascript`
  abortaba por `wait_for_pending_js` en `core/first`; se sustituyó por cobertura
  determinista sin navegador y Behat quedó como check bloqueante.

acciones:
  - "Consultados logs de GitHub Actions del run 26767455663: 5 escenarios pasaban y el único `@javascript` fallaba antes del primer Background por JS/AJAX pendiente."
  - "CI ajustada para que Behat no use `continue-on-error` y para configurar `$CFG->behat_increasetimeout = 3` tras `moodle-plugin-ci install`."
  - "Escenario Behat DEC-0017 sustituido por un caso sin `@javascript` que siembra puntuaciones por `objectid` con `track::apply_item_scores` y verifica el informe."
  - "DEC-0004 y DEC-0017 actualizados con la revisión de CI Behat."

evidencia:
  - "Run fallido base: GitHub Actions PR #8, `26767455663`, error `Javascript code and/or AJAX requests are not ready after 30 seconds (core/first)`."
  - "Run corregido: GitHub Actions PR #8, `26771584651`, matriz Moodle Plugin CI 22/22 jobs en verde, incluyendo `Behat features`."
  - "Checks finales del PR #8: Analyze, CodeQL, playground-preview y todos los jobs `test (...)` en verde."

pendientes:
  - "Mantener la verificación de navegador real del puente iframe/SCORM en TAREA-011 (manual o Playwright), fuera de Behat CI."

relacionados:
  - DEC-0004
  - DEC-0017
  - TAREA-011
